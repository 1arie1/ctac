// Safe (UNSAT): write 7 through a mutable borrow, read it back -> 7.
// Uses references; desugar before vcgen:  ttac desugar f | ttac vcgen -
entry:
  M := havoc
  i := havoc
  r, M2 := borrow_mut M[i]
  r2 := put_ref r, 7
  release r2
  x := M2[i]
  ok := x == 7
  assert ok
  halt
